# Days 1–7 (Python fundamentals) run on the standard library alone — no installs needed.
#
# The AI track (Day 8+) adds dependencies here. Install with:
#   pip install -r requirements.txt

# --- Phase 1 · Day 8 — Python for AI ---
requests                   # call web APIs (and LLMs)
httpx                      # async HTTP client (async equivalent of requests)
pydantic                   # data validation / structured output models
# python-dotenv listed below (shared with Day 9 + Day 16)

# --- Phase 1 · Day 9 — First LLM call (Groq, our default provider) ---
groq                       # Groq API: fast, free-tier Llama models — needs GROQ_API_KEY

# --- Phase 2 · Day 16 — CLI chatbot ---
# uses `groq` + `python-dotenv` (already listed above) — no new installs.

# --- Phase 2 · Day 17 — Embeddings (RAG & Memory) ---
numpy
sentence-transformers      # local, free embeddings (all-MiniLM-L6-v2) — our default
google-genai               # optional: Gemini embeddings — needs GEMINI_API_KEY
python-dotenv              # load API keys (GROQ_API_KEY, etc.) from a .env file
chromadb                   # vector database: persist embeddings, retrieve nearest docs

# --- Phase 2 · Day 19 — Streamlit (web UI for our apps) ---
streamlit                  # turn a Python script into a web app: `streamlit run app.py`
# pulls in pandas (tables) + altair automatically; groq + python-dotenv reused for the chat app

# --- Phase 2 · Day 20 — Mini-Project 1: Chat With Your Documents ---
pypdf                      # read text from .pdf uploads
python-docx                # read text from .docx uploads (note: pip name python-docx, import name docx)
# reuses streamlit + chromadb + sentence-transformers + groq + python-dotenv (all listed above)

# --- Phase 3 · Day 21 — LangChain Fundamentals ---
langchain                  # the framework: prompts, LCEL (prompt | model | parser), parsers (v1.x)
langchain-groq             # ChatGroq — LangChain's Groq integration (reuses GROQ_API_KEY)
# reuses pydantic + python-dotenv (already listed above)

# --- Phase 3 · Day 22 — LangChain in Practice: RAG & a Streamlit Chatbot ---
langchain-chroma           # Chroma vector store, the modern LangChain integration
langchain-huggingface      # local sentence-transformers embeddings through LangChain (no key)
langchain-text-splitters   # RecursiveCharacterTextSplitter (chunking for RAG)
# reuses langchain + langchain-groq + sentence-transformers + streamlit + python-dotenv (above)

# --- Phase 3 · Day 23 — LangGraph: State Machines for AI ---
langgraph                  # graphs of nodes/edges: branching, loops, memory (checkpointers) — v1.x
# reuses langchain-groq + langchain-core + python-dotenv (already listed above)
